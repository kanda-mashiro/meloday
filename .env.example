# Copy to .env and fill in from your Supabase project
# (Project Settings → API). Both values are safe to expose in the frontend;
# data isolation is enforced by row-level security (see supabase/schema.sql).
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
