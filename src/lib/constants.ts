// Reserved list id for the Inbox — a special, always-present quick-capture
// list. It is not a day and not a user custom list, so it never rolls over
// and never appears in the custom-lists carousel.
export const INBOX_LIST_ID = 'inbox'
